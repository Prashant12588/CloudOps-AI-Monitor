services:
  cloudops-app:
    build: ./app
    container_name: cloudops-app
    restart: always
    expose:
      - "5000"

  nginx:
    build: ./nginx
    container_name: cloudops-nginx
    ports:
      - "80:80"
    depends_on:
      - cloudops-app
    restart: always
